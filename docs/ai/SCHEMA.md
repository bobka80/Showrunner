# Data Dictionary (Schemas)

This document maps out the core data objects that flow through the ShowRider Logistics system.

> **Full 30-table relational model** (Vault vs Engine split): see [ENGINEERING_RULES.md](ENGINEERING_RULES.md) §6.  
> **Fragile fields** (`formula`, `containerUid`, `uid`): see [FRAGILE_ZONES.md](FRAGILE_ZONES.md).

## 1. VaultAsset (Global Assets)
This object represents the pristine definition of an asset stored in the master database. These are cached locally in the `globalAssets` array.

```json
{
  "id": "uuid-string",
  "name": "Focus Spot 4Z",
  "manufacturer": "ADJ",
  "type": "Physical", // Types: 'Physical', 'Bulk', 'Container', 'Virtual'
  "status": "Active", // Or 'Maintenance', 'Retired'
  "isConsumable": false, // If true, handled via decimal logic
  "totalQuantity": 24, // Master inventory count
  "containerType": "case-uuid", // ID of the default case this nests in
  "nestingLevel": 1, // 1=Item, 3=Case, 4=Rack
  "components": "4x Focus Spot 4Z, 1x Power Cable", // Used to calculate container capacity
  "price": 0.00, // Value for consumables expense tracking
  "tags": "[\"tag-id-1\"]"
}
```

## 2. ProjectAsset (Local Assignments)
This object represents a specific assignment of an asset to a project. These are stored in the `currentProjectAssets` array.

```json
{
  "uid": "unique-server-assignment-id", // Assigned by the database. Deleted during burst/explosion!
  "assetId": "vault-asset-uuid", // Links back to VaultAsset
  "qty": 1, // Rule: Physical types > 1 get burst into individual units. Bulk types do NOT burst, their qty remains a fluid integer. Consumables can be decimals.
  "location": "Main Stage", // Or "General"
  "formula": "Standalone", // Or a formula name, or '[SHORT] formula', or 'Auto-Container'
  "containerUid": "parent-uid", // Links to a specific assigned container
  "isAuto": false, // True if generated by Auto-Boxing
  "isShortage": false, // True if the requested qty exceeds Vault availability
  "scannedQty": 0, // Amount checked out during logistics scanning
  "overridePrice": null // Optional snapshot rental price (camelCase in JS). Used by offer/print views. Financials Phase 1.
}
```

**Naming note:** Frontend JS uses camelCase (`containerUid`, `assetId`). Google Sheets columns may use snake_case (`container_uid`, `asset_id`) for the same logical fields.

## 3. ReadinessState
This JSON payload is stored in the calendar's `extendedProps.rawDbData.readinessState`. It acts as the event's "Save File" for non-equipment states.

```json
{
  "isOutdoor": true,
  "inSofia": false,
  "distanceKm": 150,
  "coords": {
    "lat": "42.6977",
    "lng": "23.3219"
  },
  "consumablesExpense": 145.50, // Calculated automatically upon save
  "eq_depts": ["LIGHTING", "AUDIO"]
}
```

## 4. Role Matrix (Security & IAM)
Defines granular permissions and the overarching system access tier. Stored in the `Role` database sheet.
```json
{
  "Role_ID": "uuid-string",
  "Role_Name": "Finance Manager",
  "System_Access": "MANAGER", // Hierarchy: CREW -> EDITOR -> MANAGER -> ADMIN -> ROOT
  "Is_Tunneling": false,
  "fin_view_roi": true,
  "sys_manage_users": false
}
```

## 5. Crew Member (IAM Profile)
Users do NOT have a direct system access tier. They inherit it strictly from their assigned `Role_ID`. Stored in the `Crew` database sheet.
```json
{
  "uid": "uuid-string",
  "Name": "John Doe",
  "Email": "john@showrider.com",
  "Passcode": "123456",
  "Role_ID": "uuid-linking-to-role-matrix"
}
```

---

## Appendix: 30-Table Index (One-Line Reference)

**THE VAULT (Master Data):** `Crew_Roster`, `Role_Permissions`, `System_Config`, `System_Departments`, `Asset_Tags`, `Clients`, `Vehicles`, `Vendors`, `Warehouses`, `Subzones`, `Storage_Areas`, `Assets`, …

**Note on Cases / Kits / Presets:** The 30-table target model lists separate `Cases`, `Kits`, and `Presets` sheets. **Current implementation:** kit/case/preset logic is largely consolidated in the **`Assets`** sheet plus `GeneralizationEngine.generateBlueprint()` (`07c_Generalization_Engine.html`). Separate Cases/Kits/Presets tables are **planned**, not separate cached sheets in `Resources_Core.js` today.

**THE ENGINE (Transactional):** `Projects_Index`, `Project_Timelines`, `Shift_Assignments`, `Phase_Blocks`, `Dept_Overrides`, `Leave_Tracker`, `Global_Tasks`, `Task_Assignees`, `Task_Todos`, `Task_Assets`, `Project_Checklists`, `Notifications`, `Project_Assets`, `Conflict_Overrides`, `Operations_Ledger`

**Planned financial columns (Phase 1 — not in schema builder yet):** `Projects_Index.rental_days`, `Projects_Index.global_discount` — see `Project_TODO.md`.

Backend modules: `Resources_*.js` (Vault), `Logistics_*.js` (Engine). Full rules in [ENGINEERING_RULES.md](ENGINEERING_RULES.md).
